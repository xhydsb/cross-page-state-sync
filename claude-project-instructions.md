# 跨页面状态同步规范

你在前端开发中必须遵守以下规范。当你修改被多个页面或组件引用的共享数据时（如用户设置、全局配置、共享 store、全局变量），必须系统性审计所有消费方并同步更新，绝不允许只改一处就认为完成。

## 强制规则

1. 修改任何共享变量/配置前，先用搜索工具（grep/ripgrep）查找该标识符在整个项目中的所有引用位置，建立完整的"状态→消费方"映射。
2. 搜索范围包括：变量名、属性名、getter 函数名、DOM 元素 ID（如 getElementById）、模板插值表达式（如 {{ xxx }}、${xxx}）、store 的 dispatch/commit 调用点。
3. 对每个引用位置标注使用方式（读取显示 / 条件判断 / 计算依赖 / 写入初始化），然后逐一修改，不得遗漏。
4. 如果存在硬编码常量（如 const groupSize = 10），必须替换为对共享状态的引用。
5. 修改完成后，重新搜索确认无遗漏引用，并检查页面间导航流程是否正常。

## 副作用检查

修改完成后必须排查以下问题：

- 硬编码残留：页面 init()、render() 函数中是否有写死的数值或文本未走共享状态。
- 状态竞态：页面切换函数中是否存在"先赋值后检查"导致条件永远为 false 的问题。正确做法是先保存旧值再判断。
- 缓存失效：是否有局部变量或闭包捕获了旧值快照，修改后未刷新。
- 事件监听遗漏：如果状态通过 EventBus/emit 通知变更，检查所有监听者是否已注册。
- 死文本节点：HTML 中写死的静态文本（如 <span>每日10词</span>）未被 JS 动态更新，搜索变量名时不会命中，需人工审查 HTML 结构。

## 常见陷阱

- 单文件多页面架构：多个 .page div 共享同一份 JS，容易只改一个页面的渲染函数而遗漏另一个。必须搜索所有 page-xxx 相关的 DOM 操作。
- 条件守卫缺失：init 类函数在页面切换时可能被重复调用，需要守卫但确保不阻止首次进入时的初始化。
- 深层嵌套引用：值被传给子组件/子函数后层层传递，修改顶层时容易忽略传递链，需沿调用链追到底。
- 异步时序：状态修改和页面渲染之间有异步间隔时，确保渲染发生在状态更新之后。

## 验证清单

每次修改共享状态后，逐项确认：
- 所有引用点已搜索确认更新完毕
- 无残留硬编码常量
- 页面导航流程正常（A 页面改值 → B 页面正确显示）
- init() 函数读取的是共享状态而非硬编码
- 条件判断无状态竞态
- 缓存/快照已刷新
- 事件监听者已全部注册
