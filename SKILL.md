---
name: cross-page-state-sync
description: >
  多页面/多组件应用中修改共享状态时的系统性同步规范。当修改被多个页面或组件引用的共享数据（如用户设置、全局配置、共享 store）时，自动审计所有消费方并同步更新，避免遗漏。
  适用于单文件多页面 HTML 原型、Vue/React 多组件应用、以及任何存在跨页面状态依赖的前端开发场景。
  触发词："跨页面同步"、"状态同步"、"共享状态"、"改了这个页面其他页面也要改"、"遗漏页面"、"同步更新"、"cross-page"、"state sync"、"全局状态"、"多页面修改"。
version: 1.0.0
---

# 跨页面状态同步规范

## 适用场景

在以下场景中必须激活此规范：

- 修改了被多个页面/组件引用的变量、配置项或 store 字段
- 用户报告"某个页面改了但另一个页面没变"
- 新增或重构一个共享状态（如用户设置、学习计划、主题配置）
- 单文件多页面架构（如 HTML 原型中多个 `.page` 共享 JS 状态）
- 框架应用中跨路由/跨组件的状态变更

## 核心原则

**修改共享状态 = 修改所有消费方。** 绝不允许只改一处就认为完成。先审计、后动手，不要等用户逐个指出遗漏页面。

每一次修改共享状态时，都要把它当作一次"小型影响分析"来做：先搞清楚谁在用这个值，再一起改。

## 工作流

### 第 1 步：识别共享状态

在动手修改任何代码之前，先搜索目标变量/标识符在整个项目中的所有引用。

具体做法：
- 用 Grep 搜索变量名、属性名、getter 函数名
- 搜索相关的 DOM 元素 ID（如 `document.getElementById('xxx')`）
- 搜索模板中的插值表达式（如 `{{ xxx }}`、`${xxx}`）
- 对于 store/全局状态，搜索 dispatch/commit 的调用点

输出：一份"状态标识符 → 引用位置"的完整列表。

### 第 2 步：审计影响范围

对第 1 步找到的每个引用位置，标注它的**使用方式**：

| 使用方式 | 说明 | 示例 |
|---------|------|------|
| 读取显示 | 直接将值渲染到 UI | `el.textContent = planState.daily` |
| 条件判断 | 基于值做分支逻辑 | `if (groupProgress >= planState.group)` |
| 计算依赖 | 作为中间值参与运算 | `const pct = (progress / planState.group) * 100` |
| 写入/初始化 | 赋值或设置默认值 | `planState.daily = newVal` |

输出：一张影响范围表，标注每处需要做什么类型的修改。

### 第 3 步：执行同步修改

按影响范围表逐一修改所有消费方。修改时注意：

- **读取显示处**：确保绑定的数据源已更新为新值
- **条件判断处**：检查阈值/比较对象是否需要同步调整
- **计算依赖处**：确认计算公式中的变量已指向新值
- **写入/初始化处**：确保默认值和重置逻辑与新设定一致

如果存在硬编码的常量（如 `const groupSize = 10`），必须替换为对共享状态的引用。

### 第 4 步：检查副作用

修改完成后，重点排查以下副作用：

**硬编码残留** — 页面 init() 函数、render 函数中是否有写死的数值或文本，没有走共享状态。

**状态竞态** — 在页面切换函数中，是否存在"先赋值后检查"的问题。典型错误：
```javascript
// 错误：currentPage 已经被更新，条件永远为 false
function go(page) {
  currentPage = page;
  if (page === 'reinforce' && currentPage !== 'reinforce') initReinforce();
}

// 正确：先保存旧值再判断
function go(page) {
  const prevPage = currentPage;
  currentPage = page;
  if (page === 'reinforce' && prevPage !== 'reinforce') initReinforce();
}
```

**缓存/快照失效** — 是否有地方缓存了旧值的快照（如局部变量、闭包捕获），修改后没有刷新。

**事件监听遗漏** — 如果共享状态通过事件机制通知变更（如 EventBus、emit），检查所有监听者是否都已注册。

### 第 5 步：验证完整性

完成所有修改后：

1. 重新 Grep 搜索目标变量名，确认每个引用点都已更新
2. 检查页面间导航流程：从 A 页面改完状态后切到 B 页面，B 页面是否正确反映新值
3. 检查是否有"死文本"节点 — DOM 中存在但未被任何 JS 逻辑更新的静态文本
4. 如果项目有初始化流程（如 `init()`），确认初始化时读取的是共享状态而非硬编码值

## 常见陷阱

**单文件多页面架构** — 多个 `.page` div 共享同一份 JS，很容易只改了一个页面的渲染函数而忘了另一个。必须搜索所有 `page-xxx` 相关的 DOM 操作。

**"死文本"节点** — HTML 中写死的文字（如 `<span>每日10词</span>`），没有通过 JS 动态更新。这类节点在搜索时不会命中变量名，需要人工审查 HTML 结构。

**条件守卫缺失** — init 类函数在页面切换时可能被重复调用，需要加"是否已在该页面"的守卫，但要确保守卫逻辑不会阻止首次进入时的初始化。

**深层嵌套引用** — 值被传给子组件/子函数后，子层又传了更深层。修改顶层时容易忽略传递链。沿着调用链追到底。

**异步时序** — 如果状态修改和页面渲染之间存在异步间隔（如 setTimeout、Promise），可能出现"值已改但 UI 还没刷新"的中间态。确保渲染发生在状态更新之后。

## 验证清单

修改完成后，逐项确认：

- [ ] 所有引用点已通过 Grep 确认更新完毕
- [ ] 无残留硬编码常量
- [ ] 页面导航流程正常（A 页面改值 → B 页面正确显示）
- [ ] init() 函数中无硬编码，读取的是共享状态
- [ ] 条件判断中的状态竞态已修复
- [ ] 缓存/快照已刷新
- [ ] 事件监听者已全部注册
